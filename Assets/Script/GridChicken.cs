using UnityEngine;

public class GridChicken : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }
    public void PlaySound()
    {
        Debug.Log("PlaySound event triggered");
        // Play audio or do something else here
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
